#include "push_swap.h"

int main (int argc, char **argv) {

    //Take input
    
    //validate input - Check for dups, check is ints, check spaces?, check size?

    //sort
    if (argc > 1)
    {

    }

    //things to free:
    //output = ??
    
}